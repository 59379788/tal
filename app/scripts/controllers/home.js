'use strict';

/**
 * @ngdoc function
 * @name lineApp.controller:HomeCtrl
 * @description
 * # HomeCtrl
 * Controller of the lineApp
 */
angular.module('lineApp')
  .controller('HomeCtrl', ["users", "$scope", function (users, $scope) {
      
      
    
  }]);
